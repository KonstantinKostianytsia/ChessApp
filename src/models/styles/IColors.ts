export interface IColors {
  lightGrey: string;
  white: string;
  black: string;
  borderColor: string;
  textColor: string;
  buttonBackground: string;
  viewBackgroundColor: string;
  darkGreyText: string;
  selectedListItem: string;
}
